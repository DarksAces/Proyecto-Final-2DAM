<UserControl x:Class="Jovi3DReview.Views.ProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Jovi3DReview.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000">
    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Height="200" Background="{StaticResource JoviPrimaryBrush}" CornerRadius="0,0,0,0">
             <Grid>
                 <TextBlock Text="Mi Perfil" FontSize="32" FontWeight="Bold" Foreground="White" Margin="48,0,0,48" VerticalAlignment="Bottom"/>
             </Grid>
        </Border>

        <!-- Content Content -->
        <Grid Grid.Row="1" Margin="48,-40,48,0">
            <StackPanel>
                <!-- Identity Card -->
                <Border Background="White" CornerRadius="12" Padding="32" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Effect="{StaticResource MaterialDesignShadowDepth1}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                             <ColumnDefinition Width="Auto"/>
                             <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- Avatar Area -->
                        <Border Width="100" Height="100" CornerRadius="50" Background="#F0F0F0" Margin="0,0,32,0">
                            <materialDesign:PackIcon Kind="Account" Width="64" Height="64" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Gray"/>
                        </Border>
                        
                        <!-- Info Area -->
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                <TextBlock x:Name="UserNameText" Text="Cargando..." FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,12,0"/>
                                <Border Background="{StaticResource JoviYellowBrush}" CornerRadius="4" Padding="8,2" VerticalAlignment="Center">
                                    <TextBlock x:Name="UserRoleText" Text="USER" FontSize="11" FontWeight="Black" Foreground="Black"/>
                                </Border>
                            </StackPanel>
                            
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                                <materialDesign:PackIcon Kind="EmailOutline" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                <TextBlock x:Name="UserEmailText" Text="user@example.com" FontSize="14" Foreground="#64748b"/>
                            </StackPanel>

                            <Button Content="Editar Perfil" Style="{StaticResource MaterialDesignOutlinedButton}" Width="120" HorizontalAlignment="Left" IsEnabled="False" ToolTip="Proximamente"/>
                        </StackPanel>
                    </Grid>
                </Border>
                
                 <!-- Settings Section -->
                 <TextBlock Text="Configuración de Cuenta" FontSize="18" FontWeight="Bold" Margin="0,32,0,16" Foreground="{StaticResource TextPrimaryBrush}"/>
                 <Border Background="White" CornerRadius="12" Padding="24" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                     <StackPanel>
                         <!-- Password Change -->
                         <TextBlock Text="Cambiar Contraseña" FontWeight="SemiBold" Margin="0,0,0,12"/>
                         <StackPanel Orientation="Horizontal" Margin="0,0,0,24">
                             <PasswordBox x:Name="NewPasswordBox" Width="200" Height="40" VerticalContentAlignment="Center" Padding="8,0" Margin="0,0,12,0" materialDesign:HintAssist.Hint="Nueva Contraseña" Style="{StaticResource MaterialDesignOutlinedPasswordBox}"/>
                             <Button Content="Actualizar" Click="ChangePassword_Click" Style="{StaticResource MaterialDesignFlatButton}" Height="40"/>
                         </StackPanel>

                         <Separator Margin="0,0,0,24"/>

                         <!-- Notifications -->
                         <TextBlock Text="Notificaciones" FontWeight="SemiBold" Margin="0,0,0,12"/>
                         <StackPanel>
                             <DockPanel Margin="0,0,0,12">
                                 <TextBlock Text="Notificaciones por Correo" VerticalAlignment="Center" Foreground="#666"/>
                                 <ToggleButton HorizontalAlignment="Right" IsChecked="True" Style="{StaticResource MaterialDesignSwitchToggleButton}"/>
                             </DockPanel>
                             <DockPanel>
                                 <TextBlock Text="Notificaciones en la App" VerticalAlignment="Center" Foreground="#666"/>
                                 <ToggleButton HorizontalAlignment="Right" IsChecked="True" Style="{StaticResource MaterialDesignSwitchToggleButton}"/>
                             </DockPanel>
                         </StackPanel>
                     </StackPanel>
                 </Border>

                 <!-- Sign Out -->
                 <Button Style="{StaticResource MaterialDesignFlatButton}" Content="Cerrar Sesión" HorizontalAlignment="Left" Foreground="Red" Margin="0,32,0,0" Click="SignOut_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
