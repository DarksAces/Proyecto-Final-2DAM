<UserControl x:Class="Jovi3DReview.Views.HelpView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000">
    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource JoviBackgroundLightBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="32,16">
            <TextBlock Text="Centro de Ayuda" FontSize="20" FontWeight="Bold" VerticalAlignment="Center" Foreground="{StaticResource TextPrimaryBrush}"/>
        </Border>

        <ScrollViewer Grid.Row="1" Padding="32">
            <StackPanel MaxWidth="800" HorizontalAlignment="Left">
                <TextBlock Text="Preguntas Frecuentes" FontSize="18" FontWeight="Bold" Margin="0,0,0,16" Foreground="{StaticResource JoviPrimaryBrush}"/>

                <Expander Header="¿Cómo apruebo un modelo?" IsExpanded="True" Margin="0,0,0,8" Background="White" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                    <TextBlock Text="Para aprobar un modelo, navega al Dashboard, selecciona un modelo 'Pendiente' y haz clic en el botón verde 'ACEPTAR' en los detalles del modelo." Padding="16" TextWrapping="Wrap"/>
                </Expander>

                <Expander Header="¿Qué pasa si rechazo un modelo?" Margin="0,0,0,8" Background="White" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                    <TextBlock Text="Al rechazar un modelo, deberás proporcionar una razón. El modelo cambiará su estado a 'Rechazado' y el creador será notificado con el motivo del rechazo para que pueda realizar correcciones." Padding="16" TextWrapping="Wrap"/>
                </Expander>

                <Expander Header="¿Puedo cambiar mi contraseña?" Margin="0,0,0,8" Background="White" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                    <TextBlock Text="Sí, ve a tu Perfil (clic en tu nombre en la barra lateral) y utiliza la sección 'Cambiar Contraseña'." Padding="16" TextWrapping="Wrap"/>
                </Expander>

                <TextBlock Text="Soporte Técnico" FontSize="18" FontWeight="Bold" Margin="0,24,0,16" Foreground="{StaticResource JoviPrimaryBrush}"/>
                <Border Background="White" CornerRadius="8" Padding="16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="Si tienes problemas técnicos, contacta a IT:" Margin="0,0,0,8"/>
                        <TextBlock Text="Email: soporte@jovi.es" FontWeight="Bold"/>
                        <TextBlock Text="Tel: +34 93 555 12 34" FontWeight="Bold"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
