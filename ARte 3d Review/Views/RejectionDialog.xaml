<Window x:Class="Jovi3DReview.Views.RejectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Rechazar Modelo" Height="450" Width="500"
        WindowStartupLocation="CenterScreen" WindowStyle="None" ResizeMode="NoResize"
        Background="Transparent" AllowsTransparency="True">
    
    <Border Background="White" CornerRadius="12" ClipToBounds="True" BorderBrush="#E0E0E0" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Header -->
                <RowDefinition Height="Auto"/> <!-- Warning -->
                <RowDefinition Height="*"/>    <!-- Form -->
                <RowDefinition Height="Auto"/> <!-- Footer -->
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="24">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Rechazar Modelo" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                    <Button Grid.Column="1" Style="{StaticResource MaterialDesignIconForegroundButton}" Width="24" Height="24" Click="Cancel_Click">
                        <materialDesign:PackIcon Kind="Close" Foreground="{StaticResource TextSecondaryBrush}"/>
                    </Button>
                </Grid>
            </Border>

            <!-- Warning Info -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="24,24,24,0">
                <Border Background="#FFF4E5" CornerRadius="20" Width="40" Height="40" Margin="0,0,16,0">
                    <materialDesign:PackIcon Kind="AlertCircleOutline" Foreground="#F57C00" Width="24" Height="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <TextBlock Text="Vas a rechazar el modelo 'Plastilina Neon 500g Scan'. Esta acción notificará al creador." Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" TextWrapping="Wrap" Width="360"/>
            </StackPanel>

            <!-- Form -->
            <StackPanel Grid.Row="2" Margin="24">
                <TextBlock Text="Motivo del rechazo" FontWeight="Bold" FontSize="12" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,8"/>
                <ComboBox x:Name="ReasonCombo" Padding="12" Background="{StaticResource JoviBackgroundLightBrush}" BorderThickness="0" materialDesign:TextFieldAssist.DecorationVisibility="Hidden">
                    <ComboBoxItem Content="Problemas de Geometría"/>
                    <ComboBoxItem Content="Texturas de Baja Resolución"/>
                    <ComboBoxItem Content="Escala Incorrecta"/>
                    <ComboBoxItem Content="Otro"/>
                </ComboBox>
                
                <TextBlock Text="Comentarios Adicionales" FontWeight="Bold" FontSize="12" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,16,0,8"/>
                <TextBox x:Name="CommentsBox" Height="100" Padding="12" Background="{StaticResource JoviBackgroundLightBrush}" BorderThickness="0" materialDesign:TextFieldAssist.DecorationVisibility="Hidden" materialDesign:HintAssist.Hint="Explica en detalle los cambios necesarios..." VerticalContentAlignment="Top" TextWrapping="Wrap" AcceptsReturn="True"/>
            </StackPanel>

            <!-- Footer -->
            <Border Grid.Row="3" Background="{StaticResource JoviBackgroundLightBrush}" Padding="24">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Cancelar" Style="{StaticResource MaterialDesignOutlinedButton}" BorderBrush="{StaticResource BorderBrush}" Foreground="{StaticResource TextPrimaryBrush}" Height="40" Margin="0,0,12,0" Click="Cancel_Click"/>
                    <Button Content="Rechazar Modelo" Background="{StaticResource JoviPrimaryBrush}" BorderBrush="{StaticResource JoviPrimaryBrush}" Height="40" Click="Confirm_Click"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
