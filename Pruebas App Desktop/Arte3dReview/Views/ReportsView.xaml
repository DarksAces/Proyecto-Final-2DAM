<UserControl x:Class="Jovi3DReview.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000">
    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource ARteBackgroundLightBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="32,16">
            <Grid>
                <TextBlock Text="Reportes y Estadísticas" FontSize="20" FontWeight="Bold" VerticalAlignment="Center" Foreground="{StaticResource TextPrimaryBrush}"/>
                
                <!-- Time Filters -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock Text="Filtrar por:" VerticalAlignment="Center" Margin="0,0,12,0" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" FontWeight="Bold"/>
                    <Border Background="White" CornerRadius="8" Padding="2" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                        <StackPanel Orientation="Horizontal">
                            <RadioButton x:Name="FilterWeekly" Style="{StaticResource MaterialDesignTabRadioButton}" Content="Semanal" IsChecked="True" Padding="12,4" Checked="Filter_Checked"/>
                            <RadioButton x:Name="FilterMonthly" Style="{StaticResource MaterialDesignTabRadioButton}" Content="Mensual" Padding="12,4" Checked="Filter_Checked"/>
                            <RadioButton x:Name="FilterYearly" Style="{StaticResource MaterialDesignTabRadioButton}" Content="Anual" Padding="12,4" Checked="Filter_Checked"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="1" Padding="32">
            <StackPanel>
                <WrapPanel>
                    <!-- Card Total -->
                    <Border Width="220" Background="White" CornerRadius="12" Padding="20" Margin="0,0,16,16" Effect="{StaticResource MaterialDesignShadowDepth1}">
                        <StackPanel>
                            <TextBlock Text="Total Modelos" FontSize="14" Foreground="{StaticResource TextSecondaryBrush}"/>
                            <TextBlock x:Name="TotalCount" Text="0" FontSize="32" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,8,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- Card Approved -->
                    <Border Width="220" Background="White" CornerRadius="12" Padding="20" Margin="0,0,16,16" Effect="{StaticResource MaterialDesignShadowDepth1}">
                        <StackPanel>
                            <TextBlock Text="Aprobados" FontSize="14" Foreground="{StaticResource TextSecondaryBrush}"/>
                            <TextBlock x:Name="ApprovedCount" Text="0" FontSize="32" FontWeight="Bold" Foreground="{StaticResource ARteGreenBrush}" Margin="0,8,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- Card Rejected -->
                    <Border Width="220" Background="White" CornerRadius="12" Padding="20" Margin="0,0,16,16" Effect="{StaticResource MaterialDesignShadowDepth1}">
                        <StackPanel>
                            <TextBlock Text="Rechazados" FontSize="14" Foreground="{StaticResource TextSecondaryBrush}"/>
                            <TextBlock x:Name="RejectedCount" Text="0" FontSize="32" FontWeight="Bold" Foreground="{StaticResource ARtePrimaryBrush}" Margin="0,8,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- Card Pending -->
                    <Border Width="220" Background="White" CornerRadius="12" Padding="20" Margin="0,0,16,16" Effect="{StaticResource MaterialDesignShadowDepth1}">
                        <StackPanel>
                            <TextBlock Text="Pendientes" FontSize="14" Foreground="{StaticResource TextSecondaryBrush}"/>
                            <TextBlock x:Name="PendingCount" Text="0" FontSize="32" FontWeight="Bold" Foreground="{StaticResource ARteSecondaryBrush}" Margin="0,8,0,0"/>
                        </StackPanel>
                    </Border>
                </WrapPanel>

                <!-- CHART SECTION -->
                <Border Background="White" CornerRadius="16" Padding="24" Margin="0,8,0,0" Effect="{StaticResource MaterialDesignShadowDepth1}">
                    <StackPanel>
                        <TextBlock Text="Actividad de Revisiones" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,24"/>
                        <Grid Height="400">
                            <lvc:CartesianChart x:Name="MainChart" Series="{Binding Series}" XAxes="{Binding XAxes}" YAxes="{Binding YAxes}" LegendPosition="Right"/>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
