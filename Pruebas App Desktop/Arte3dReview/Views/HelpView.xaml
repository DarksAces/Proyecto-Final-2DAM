<UserControl x:Class="Jovi3DReview.Views.HelpView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000">
    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource ARteBackgroundLightBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="32,16">
            <TextBlock Text="{DynamicResource StrCentroAyuda}" FontSize="20" FontWeight="Bold" VerticalAlignment="Center" Foreground="{StaticResource TextPrimaryBrush}"/>
        </Border>

        <ScrollViewer Grid.Row="1" Padding="32">
            <StackPanel MaxWidth="800" HorizontalAlignment="Left">
                <TextBlock Text="{DynamicResource StrPreguntasFrecuentes}" FontSize="18" FontWeight="Bold" Margin="0,0,0,16" Foreground="{StaticResource ARtePrimaryBrush}"/>

                <Expander Header="{DynamicResource StrFaq1Pregunta}" IsExpanded="True" Margin="0,0,0,8" Background="White" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                    <TextBlock Text="{DynamicResource StrFaq1Respuesta}" Padding="16" TextWrapping="Wrap"/>
                </Expander>

                <Expander Header="{DynamicResource StrFaq2Pregunta}" Margin="0,0,0,8" Background="White" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                    <TextBlock Text="{DynamicResource StrFaq2Respuesta}" Padding="16" TextWrapping="Wrap"/>
                </Expander>

                <Expander Header="{DynamicResource StrFaq3Pregunta}" Margin="0,0,0,8" Background="White" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                    <TextBlock Text="{DynamicResource StrFaq3Respuesta}" Padding="16" TextWrapping="Wrap"/>
                </Expander>

                <TextBlock Text="{DynamicResource StrSoporteTecnico}" FontSize="18" FontWeight="Bold" Margin="0,24,0,16" Foreground="{StaticResource ARtePrimaryBrush}"/>
                <Border Background="White" CornerRadius="8" Padding="16" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource StrSoporteDesc}" Margin="0,0,0,8"/>
                        <TextBlock Text="{DynamicResource StrSoporteEmail}" FontWeight="Bold"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
