<UserControl x:Class="Jovi3DReview.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000">
    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{DynamicResource ARteBackgroundLightBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1" Padding="32,16">
            <TextBlock Text="{DynamicResource StrConfiguracion}" FontSize="20" FontWeight="Bold" VerticalAlignment="Center" Foreground="{DynamicResource TextPrimaryBrush}"/>
        </Border>

        <!-- Wrapped in ScrollViewer to enable Padding and scrolling -->
        <ScrollViewer Grid.Row="1" Padding="32">
            <StackPanel MaxWidth="800" HorizontalAlignment="Left">
                
                <!-- Appearance Section -->
                <GroupBox Header="{DynamicResource StrApariencia}" Style="{StaticResource MaterialDesignCardGroupBox}" Margin="0,0,0,24">
                    <Grid Margin="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="{DynamicResource StrModoOscuro}" FontWeight="Bold"/>
                            <TextBlock Text="{DynamicResource StrModoOscuroDesc}" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}"/>
                        </StackPanel>
                        <ToggleButton Grid.Column="1" IsChecked="{Binding IsDarkMode}" Style="{StaticResource MaterialDesignSwitchToggleButton}"/>
                    </Grid>
                </GroupBox>

                <!-- Preferences Section -->
                <GroupBox Header="{DynamicResource StrIdioma}" Style="{StaticResource MaterialDesignCardGroupBox}" Margin="0,0,0,24">
                    <StackPanel Margin="16">
                         <TextBlock Text="{DynamicResource StrIdiomaInterfaz}" FontWeight="Bold" Margin="0,0,0,8"/>
                        <ComboBox SelectedValue="{Binding SelectedLanguage}" SelectedValuePath="Content" Style="{StaticResource MaterialDesignOutlinedComboBox}" Width="200" HorizontalAlignment="Left">
                            <ComboBoxItem Content="Español"/>
                            <ComboBoxItem Content="English"/>
                        </ComboBox>
                    </StackPanel>
                </GroupBox>

                <!-- Account Section -->
                <GroupBox Header="{DynamicResource StrCuenta}" Style="{StaticResource MaterialDesignCardGroupBox}" Margin="0,0,0,24">
                    <StackPanel Margin="16">
                        <Button Content="{DynamicResource StrCerrarApp}" Style="{StaticResource MaterialDesignFlatButton}" Foreground="{DynamicResource ARtePrimaryBrush}" 
                                HorizontalAlignment="Left" Command="{Binding CloseApplicationCommand}"/>
                    </StackPanel>
                </GroupBox>

                <!-- Maintenance Section -->
                <GroupBox Header="{DynamicResource StrMantenimiento}" Style="{StaticResource MaterialDesignCardGroupBox}" Margin="0,0,0,24">
                    <StackPanel Margin="16">
                        <DockPanel LastChildFill="False">
                            <StackPanel>
                                <TextBlock Text="{DynamicResource StrLimpiarCache}" FontWeight="Bold"/>
                                <TextBlock Text="{DynamicResource StrLimpiarCacheDesc}" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}"/>
                            </StackPanel>
                            <Button DockPanel.Dock="Right" Content="{DynamicResource StrLimpiar}" Style="{StaticResource MaterialDesignOutlinedButton}" Command="{Binding ClearCacheCommand}"/>
                        </DockPanel>
                    </StackPanel>
                </GroupBox>

                <!-- About -->
                <GroupBox Header="{DynamicResource StrAcercaDe}" Style="{StaticResource MaterialDesignCardGroupBox}">
                    <StackPanel Margin="16">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                            <Image Source="pack://application:,,,/Assets/ARte.png" Width="48" Height="48" Margin="0,0,16,0"/>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="ARte Review" FontWeight="Bold" FontSize="16"/>
                                <TextBlock Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12">
                                    <Run Text="{DynamicResource StrVersion}"/>
                                    <Run Text=" 1.2.0 (Stable)"/>
                                </TextBlock>
                            </StackPanel>
                        </StackPanel>
                        <TextBlock Text="{DynamicResource StrDescripcionApp}" FontSize="13" Margin="0,0,0,8"/>
                        <TextBlock Text="{DynamicResource StrCopyright}" Foreground="#64748b" FontSize="12"/>
                    </StackPanel>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
