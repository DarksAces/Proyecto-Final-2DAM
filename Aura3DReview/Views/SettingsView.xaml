<UserControl x:Class="Jovi3DReview.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000">
    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource JoviBackgroundLightBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="32,16">
            <TextBlock Text="Configuración" FontSize="20" FontWeight="Bold" VerticalAlignment="Center" Foreground="{StaticResource TextPrimaryBrush}"/>
        </Border>

        <!-- Wrapped in ScrollViewer to enable Padding and scrolling -->
        <ScrollViewer Grid.Row="1" Padding="32">
            <StackPanel MaxWidth="600" HorizontalAlignment="Left">
                <!-- Apariencia (Removed per user request) -->


                <!-- Language -->
                <GroupBox Header="Idioma" Style="{StaticResource MaterialDesignCardGroupBox}" Margin="0,0,0,24">
                    <StackPanel Margin="16">
                        <TextBlock Text="Idioma de la Interfaz" FontWeight="Bold" Margin="0,0,0,8"/>
                        <ComboBox SelectedIndex="0" Style="{StaticResource MaterialDesignOutlinedComboBox}">
                            <ComboBoxItem Content="Español"/>
                            <ComboBoxItem Content="English"/>
                            <ComboBoxItem Content="Català"/>
                        </ComboBox>
                    </StackPanel>
                </GroupBox>

                <!-- About -->
                <GroupBox Header="Acerca de" Style="{StaticResource MaterialDesignCardGroupBox}">
                    <StackPanel Margin="16">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                            <Border Width="48" Height="48" CornerRadius="12" Background="{StaticResource JoviPrimaryBrush}" Margin="0,0,16,0">
                                <TextBlock Text="J" Foreground="White" FontWeight="Bold" FontSize="28" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="JOVI 3D Review" FontWeight="Bold" FontSize="18"/>
                                <TextBlock Text="Versión 1.0.0" Foreground="{StaticResource TextSecondaryBrush}"/>
                            </StackPanel>
                        </StackPanel>
                        <TextBlock Text="© 2026 Jovi S.A. Todos los derechos reservados." Foreground="#64748b"/>
                    </StackPanel>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
