<UserControl x:Class="Jovi3DReview.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000">
    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource JoviBackgroundLightBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="32,16">
            <TextBlock Text="Reportes y Estadísticas" FontSize="20" FontWeight="Bold" VerticalAlignment="Center" Foreground="{StaticResource TextPrimaryBrush}"/>
        </Border>

        <ScrollViewer Grid.Row="1" Padding="32">
            <WrapPanel>
                <!-- Card Total -->
                <Border Width="240" Background="White" CornerRadius="12" Padding="20" Margin="0,0,24,24" Effect="{StaticResource MaterialDesignShadowDepth1}">
                    <StackPanel>
                        <TextBlock Text="Total Modelos" FontSize="14" Foreground="{StaticResource TextSecondaryBrush}"/>
                        <TextBlock x:Name="TotalCount" Text="0" FontSize="36" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Card Approved -->
                <Border Width="240" Background="White" CornerRadius="12" Padding="20" Margin="0,0,24,24" Effect="{StaticResource MaterialDesignShadowDepth1}">
                    <StackPanel>
                        <TextBlock Text="Aprobados" FontSize="14" Foreground="{StaticResource TextSecondaryBrush}"/>
                        <TextBlock x:Name="ApprovedCount" Text="0" FontSize="36" FontWeight="Bold" Foreground="{StaticResource JoviGreenBrush}" Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Card Rejected -->
                <Border Width="240" Background="White" CornerRadius="12" Padding="20" Margin="0,0,24,24" Effect="{StaticResource MaterialDesignShadowDepth1}">
                    <StackPanel>
                        <TextBlock Text="Rechazados" FontSize="14" Foreground="{StaticResource TextSecondaryBrush}"/>
                        <TextBlock x:Name="RejectedCount" Text="0" FontSize="36" FontWeight="Bold" Foreground="{StaticResource JoviPrimaryBrush}" Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Card Pending -->
                <Border Width="240" Background="White" CornerRadius="12" Padding="20" Margin="0,0,24,24" Effect="{StaticResource MaterialDesignShadowDepth1}">
                    <StackPanel>
                        <TextBlock Text="Pendientes" FontSize="14" Foreground="{StaticResource TextSecondaryBrush}"/>
                        <TextBlock x:Name="PendingCount" Text="0" FontSize="36" FontWeight="Bold" Foreground="{StaticResource JoviYellowBrush}" Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>
            </WrapPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
