name: object_capture_3d
description: A new Flutter project for 3D object capture.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  camera: ^0.10.5
  http: ^1.1.0
  path_provider: ^2.1.1
  permission_handler: ^11.0.1
  model_viewer_plus: ^1.6.0
  flutter_dotenv: ^5.1.0
  uuid: ^4.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
