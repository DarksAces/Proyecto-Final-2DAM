name: jovi_app
description: "A new Flutter project for Jovi AR World."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # =======================================================
  # 🔑 FIREBASE & CLOUD SERVICES (NUEVAS DEPENDENCIAS)
  # =======================================================
  firebase_core: ^2.27.0    # 1. Base: Necesario para inicializar Firebase
  cloud_firestore: ^4.17.5   # 2. Base de datos: Para guardar metadatos (lat/lng, título)
  firebase_storage: ^11.7.5   # 3. Almacenamiento: Para subir fotos (.jpg) y modelos (.glb)
  image_picker: ^1.1.2        # 4. Utilidad: Para seleccionar fotos de la galería
  
  # Librerías para el Mapa y GPS (EXISTENTES)
  mapbox_maps_flutter: 2.12.0
  geolocator: ^12.0.0
  permission_handler: ^12.0.1

  # Librería para la Cámara (AR)
  camera: ^0.11.3

  # Estilos e Iconos (EXISTENTES)
  google_fonts: ^6.1.0
  lucide_icons: ^0.257.0
  
  # Utilidades (EXISTENTES)
  cached_network_image: ^3.3.0
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  
  # 🖼️ ASSETS (Incluimos el directorio completo para el avatar)
  assets:
    - assets/